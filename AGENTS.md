# Agent Guidance

- This repository is a Home Assistant integration for detecting appliance cycles (washer/dryer/dishwasher).
- Core code lives in `custom_components/appliance_cycle/`; update docs in `README.md` if behavior, entities, or workflows change.
- Keep this file up to date with any changes to repository workflows, structure, or contributor expectations.
- Follow existing patterns and naming in the integration when adding or editing entities, services, or options.
- Release automation includes stable releases from `main` merges and prereleases from `develop` merges; prereleases use `X.Y.Z-beta.N` tags.
